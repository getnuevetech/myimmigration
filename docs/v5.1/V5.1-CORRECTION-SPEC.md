# V5.1 Development Correction Specification

**Status:** Phase 0 — revised golden fixture (v2); awaiting freeze approval before Phase F  
**Date:** 2026-08-29 (rev 2)  
**Sits on:** V5 Situation Explanation & Presentation Rules + shipped V5 Phases 1–6  
**Golden fixture:** [`golden-vawa-prima-facie.json`](./golden-vawa-prima-facie.json) (`fixture_version`: **2**)

---

## 1. Purpose

V5 delivered the correct **architecture** (situation brief → case-type lock → six-section customer presentation → printable report). The six-page VAWA review showed the **customer shell is ahead of document → fact → action integrity**.

V5.1 does **not** redesign the UI. It makes the pipeline feeding that UI **correct, stale-proof, deduplicated, isolated, gated, and operationally reliable**.

### Diagnosis (accepted)

| Layer | V5 state | V5.1 target |
| --- | --- | --- |
| Presentation hierarchy | Working | Keep (non-regression) |
| Prima facie meaning | Working | Keep |
| Document classification / takeaways | Broken or mis-mapped | Correct type + label + dedupe |
| Fact ledger | Receipt can still leave filing “reported” | Provenance + verified inheritance + invalidation |
| Isolation | Ranking lock ≠ copy/recommendation lock | Three locks + positive/negative fixtures |
| Next actions | Generic form/review steps | Unknown/conflict-driven ranking |
| Analysis plan honesty | “Options review / processing not needed” on doc cases | Honest skip reasons only |
| Orchestration | 78 failed calls / many runs observed | One logical analysis ID + hard ceilings |
| Approval | Soft / human discovery | Deterministic BLOCK / WARNING + audit trail |

---

## 2. What V5 fixed (non-regressions)

These must remain true after every V5.1 phase:

1. Customer first screen names **VAWA self-petition**, primary **I-360**, related **adjustment / I-485** when applicable.
2. Facts carry **verified / reported / unknown** markers (customer-facing).
3. Prima facie explained as **preliminary**, **not final I-360 approval**, **not a green card**.
4. Anti-recommendation **allowed** when relevant: do **not** treat marriage alone as reason to file a **new I-130** when VAWA I-360 is locked (not a mandatory form phrase on every VAWA case).
5. Six-section hierarchy on analyze page and printable report.
6. Staff-only readiness %, most-likely dumps, old contract details.
7. Existing fixture gates for brief / lock / presentation / prompts / report continue to pass unless intentionally superseded by V5.1 fixtures.

---

## 3. Defects from the six-page VAWA report (mapped)

| # | Observed defect | Likely layer(s) | V5.1 phase |
| --- | --- | --- | --- |
| 1 | I-360 receipt / declaration shown as Identity & Entry | Classifier **and/or** takeaway fallback on `identity`/`docKind` | A |
| 2 | I-360 filing still “You told us” despite receipt upload | Brief built before type settled; missing invalidation; provenance not applied | B |
| 3 | I-130 / I-589 instructional bleed in evidence “why it matters” | Presentation/retrieval copy without recommendation/presentation locks | C |
| 4 | Duplicate Prima Facie rows | Missing evidence dedupe | A |
| 5 | Generic next actions (“Review I-360”, “Ask a follow-up”) | Action generator prefers pathSteps/forms over unknowns | D |
| 6 | “Document processing is not needed for this options review” | Analysis plan skip reason when docs exist / filed lock | E (+ plan honesty in A/B) |
| 7 | 78 failed model calls / many analysis runs | Orchestration retries, fallbacks, duplicate jobs, re-analyze loops | F |

**Trace rule:** Before changing a layer, prove whether the stored `documentType` is wrong or the renderer mapped a correct type to a wrong label.

---

## 4. Delivery sequence (locked)

```
0  Correction Spec + golden VAWA JSON     ← this document
F  Orchestration reliability
A  Document classification + dedupe
B  Fact ledger + provenance + invalidation
C  Retrieval / Presentation / Recommendation locks
E  Approval gate (BLOCK / WARNING) + audit trail
D  Unknown / conflict-driven next actions
G  Multi-fixture pack (positive + negative)
```

**Rule:** No Phase F or feature code until this Phase 0 spec and golden JSON are **approved in writing**.

---

## 5. Core data model requirements

### 5.1 Fact with provenance

Every ledger fact MUST carry explicit provenance. **Production joins on immutable IDs**, not filenames:

```json
{
  "fact_id": "FORM_I360_FILED",
  "status": "VERIFIED",
  "value": true,
  "sources": [
    {
      "source_type": "DOCUMENT",
      "document_id": "doc_…",
      "content_hash": "sha256:…",
      "document_type": "uscis_i360_receipt_notice",
      "extracted_field": "form_number",
      "source_page": 1,
      "authority_rank": "USCIS_GOVERNMENT_DOCUMENT"
    }
  ],
  "superseded_by": null,
  "as_of": "ISO-8601"
}
```

Golden fixtures may use friendly `alias_file_name` for readability; the contract requires `document_id` + `content_hash` in production.

Customer “Verified from your documents” MUST be traceable to at least one DOCUMENT source.

**Resolution vs promotion:** Uploading an I-485 receipt **resolves** unknown `I485_RECEIPT` and **promotes** `FORM_I485_FILED` from `REPORTED` → `VERIFIED`. These are distinct operations.

**Marriage granularity:** Model `MARRIAGE_EXISTS` (often verifiable from civil record) separately from `SPOUSE_US_CITIZEN` (often reported until citizenship evidence exists).

### 5.2 Evidence authority hierarchy (highest → lowest)

1. USCIS / government document  
2. Filed form / official receipt  
3. Professional record  
4. Customer-uploaded supporting document  
5. Customer statement  
6. AI inference  

When sources disagree, **do not silently pick**. Create a **conflict** (see §5.3). Higher authority wins for `VERIFIED` value; lower authority may remain as `REPORTED` alongside.

### 5.3 Evidence gaps vs conflicts

| Kind | Meaning | Example |
| --- | --- | --- |
| **EVIDENCE_GAP / UNVERIFIED** | Affirmative claim or needed status lacks verifying evidence | Customer says I-485 filed; no receipt yet. Or: receipt exists; final decision unknown. |
| **CONFLICT** | Two **affirmative incompatible** values on the same subject | Customer says approved **vs** USCIS denial notice |

An absent document is **not** `source_b` of a conflict. Do not manufacture conflicts from gaps.

Conflicts (when real) are first-class and connect evidence → action:

```json
{
  "conflict_id": "conf_…",
  "subject": "I485_FINAL_DECISION",
  "source_a": { "type": "USER_STATEMENT", "value": "APPROVED" },
  "source_b": { "type": "USCIS_DOCUMENT", "value": "DENIED" },
  "severity": "WARNING",
  "resolution_status": "UNRESOLVED",
  "required_action": "UPLOAD_OR_CONFIRM_CONTROLLING_I485_NOTICE"
}
```

### 5.4 Event timeline vs current-state supersession

**Events accumulate** (receipt remains a true historical event after prima facie).  
**Current case-state posture** is what gets superseded:

`FILED_PENDING → PRIMA_FACIE_PENDING → RFE_PENDING → UNDER_REVIEW → APPROVED|DENIED|WITHDRAWN`

Use fact id **`I360_FINAL_DECISION`** with enum value `APPROVED | DENIED | WITHDRAWN | UNKNOWN` — never assume approval.

### 5.5 Fact / interpretation / implication (customer conclusions)

| Layer | Meaning | Provenance |
| --- | --- | --- |
| **FACT** | What happened | Evidence sources required |
| **LEGAL INTERPRETATION** | Process meaning | **`authority_required: true`**; USCIS/DOJ/DOS/EOIR (or equivalent) must support claims such as prima facie meaning / not final I-360 approval |
| **IMPLICATION FOR GOAL** | Meaning for this customer’s goal | Derived from locked matter + verified/reported/unknown state |

AI must not invent facts or unsupported legal interpretation.

### 5.6 Dependency invalidation

Trigger: `DOCUMENT_CLASSIFICATION_CHANGED` (and equivalent evidence identity changes).

Cascade: fact ledger → case brief → affected analysis → customer presentation.

Coalesce window: **30 seconds** (configurable).  
`stale_customer_output_allowed`: **false**.  
Gate rule: `BLOCK-STATE-STALE-BRIEF-AFTER-EVIDENCE-CHANGE` / `BLOCK-STATE-STALE-DERIVED-OUTPUT`.

### 5.7 Action priority formula

Scale **1–5** per dimension (1 = minimal, 5 = critical/blocking).

```
priority_score =
  (legal_case_materiality * 3)
+ (evidence_gap_importance * 3)
+ (deadline_urgency * 2)
+ (goal_relevance * 3)
+ (ability_to_resolve * 2)
```

Rank by `priority_score` descending. Fixture action order MUST match computed scores (no hand-rank that contradicts the formula).

Generic `REVIEW_FORM` / `ASK_FOLLOW_UP` must not outrank material gap-resolving actions.

---

## 6. System invariants

These are **code-enforced**, not prompt hopes.

| ID | Invariant |
| --- | --- |
| INV-FACT-01 | IF qualifying `I360_RECEIPT` evidence exists THEN `FORM_I360_FILED` MUST NOT remain `REPORTED`-only |
| INV-LOCK-01 | IF primary = VAWA_I360 AND no independent asylum matter THEN I-589 / country-conditions MUST NOT enter customer output as requirements or recommendations |
| INV-LOCK-02 | IF primary = VAWA_I360 THEN I-130 MUST NOT appear as recommended new filing / evidence framework / primary pathway (anti-recommendation and contrast allowed) |
| INV-PLAN-01 | IF immigration documents exist in evidence set THEN plan MUST NOT claim “Document processing is not needed for this options review” |
| INV-DEDUP-01 | IF same `contentHash` (or equivalent identity) appears twice THEN customer evidence count = 1 |
| INV-ACT-01 | IF unresolved material unknowns/conflicts exist THEN generic `REVIEW_FORM` / `ASK_FOLLOW_UP` MUST NOT outrank actions that resolve those gaps |
| INV-AUTH-01 | Lower-authority source MUST NOT overwrite higher-authority verified value without a conflict object |

---

## 7. Locks (three levels)

### 7.1 Retrieval lock
Controls which authorities, forms, and evidence frameworks may enter analysis context.

### 7.2 Presentation lock
Controls which unrelated terminology may appear in **customer-facing** text.

### 7.3 Recommendation lock
Controls which forms/pathways may be **recommended as next path**.

**I-130 / I-589 policy (VAWA-locked):**

| May appear | May NOT appear |
| --- | --- |
| Historical filing the user stated | Recommended new filing |
| Contrast / explanation | Evidence framework for this case |
| Explicit **anti-recommendation** when relevant | Primary pathway |

**Anti-I-130 is allowed, not globally required.** Use `allowed_anti_recommendations` (e.g. `DO_NOT_START_NEW_I130_FOR_MARRIAGE_ALONE`). Require that language only when a competing I-130 path would otherwise be recommended, or when the customer question / analysis is correcting a marriage→I-130 misconception. This golden fixture sets `required_in_this_fixture: true` as a V4 regression guard—not a product-wide habit.

**Forbidden simplistic rule:** `VAWA && string.includes("I-130") → FAIL`  
**Required rule:** classify mention role; fail only on prohibited roles.

---

## 8. Dependency invalidation (detail)

See also §5.6. Async uploads must not leave stale briefs.

### Trigger
`DOCUMENT_CLASSIFICATION_CHANGED` (and equivalent evidence identity changes)

### Cascade
1. Invalidate evidence-derived facts for affected docs  
2. Rebuild fact ledger (provenance + gaps/conflicts + event timeline + current-state posture)  
3. Rebuild situation brief  
4. Rerun **affected** analysis stages under the **same logical analysis revision** (or spawn a new logical revision child—never silent drift)  
5. Regenerate presentation / approved customer state  

### Coalesce / debounce
Multiple classification events within **30s** (configurable) coalesce into **one** rebuild.  
`stale_customer_output_allowed: false`.

---

## 9. Action priority (deterministic formula)

Phase D ranks by unresolved material gaps/conflicts using the **additive** formula in §5.7 (not a hand-declared rank independent of scores).

Scale **1–5** on every dimension (see golden `action_priority_model.scale`).

Golden expected order (scores must compute to this ranking):

1. `UPLOAD_I485_RECEIPT` (resolve `I485_RECEIPT` + promote `FORM_I485_FILED`)  
2. `UPLOAD_POST_PRIMA_FACIE_NOTICES`  
3. `UPLOAD_WAIVER_NOTICE`  
4. `CONFIRM_I485_PROCEDURAL_STATUS`  
5. `UPDATE_GREEN_CARD_PATH_EXPLANATION`  

Generic `REVIEW_FORM_I360` / `ASK_FOLLOW_UP` must not outrank those.

Canonical numbers: [`golden-vawa-prima-facie.json`](./golden-vawa-prima-facie.json) → `next_actions_ordered`.

---

## 10. Approval gate (BLOCK vs WARNING) + audit trail

### 10.1 BLOCK (fail closed — no customer approve)

Required rule ids for this fixture (non-exhaustive product set may grow):

| Rule id | Intent |
| --- | --- |
| `BLOCK-DOC-MISCLASS-I360-RECEIPT-AS-IDENTITY` | Receipt ≠ identity/travel |
| `BLOCK-DOC-MISCLASS-DECLARATION-AS-IDENTITY` | Declaration ≠ identity/travel |
| `BLOCK-FACT-I360-REPORTED-DESPITE-RECEIPT` | Receipt must promote I-360 filing to verified |
| `BLOCK-LOCK-I589-IN-VAWA` | No I-589 / country-conditions under VAWA lock |
| `BLOCK-LOCK-I130-RECOMMENDED-IN-VAWA` | No new I-130 recommendation under VAWA lock |
| `BLOCK-DEDUP-DUPLICATE-EVIDENCE-ROW` | Same document identity → one customer row |
| `BLOCK-PLAN-DOCS-SKIPPED-WHILE-USED` | No “processing not needed” when docs drive conclusions |
| `BLOCK-STATE-STALE-BRIEF-AFTER-EVIDENCE-CHANGE` | Brief rebuilt after classification change |
| `BLOCK-STATE-STALE-DERIVED-OUTPUT` | No stale derived customer output after evidence change |
| `BLOCK-AUTHORITY-LEGAL-INTERPRETATION-UNSUPPORTED` | Legal meaning needs allowed authority support |

### 10.2 WARNING (operable; staff-visible)
Examples: missing medical; unresolved waiver type; genuine conflict object; incomplete timeline; evidence gap (not conflict); secondary authority unavailable.

### 10.3 Gate audit trail (required)
On block or warn, persist: `gate_result`, `rule_ids[]`, human-readable reasons, analysis/revision ids, timestamp.  
Staff override (if enabled later): `override_by`, `override_time`, `override_reason`, `previous_gate_result`. No silent overrides.

---

## 11. Reliability ceilings (Phase F targets)

Per **single user-requested analysis** (golden `reliability_ceilings_for_this_fixture`):

| Metric | Ceiling |
| --- | --- |
| Logical analyses per user request | **1** |
| Max model attempts per stage | **2** |
| Max fallback models per stage | **1** |
| Max structured-output retries | **1** |
| Max retry children | **3** |
| Max **total** model calls per analysis | **24** |
| Max **total** failed model calls | **4** |
| Max wall-clock seconds | **180** |
| Uncaught model failures in customer output | **0** |
| Duplicate concurrent logical analyses | **0** |
| Target success rate | **0.95** |
| Token budget hint | **250000** |

Stage-local retry limits alone are insufficient: aggregate call/failure/latency ceilings prevent another 78-call storm.

**Logical analysis ID** owns: retries, parse failures, fallbacks, child calls, and the customer-visible revision pointer.

---

## 12. Machine-readable golden VAWA fixture (v2)

Canonical file: [`golden-vawa-prima-facie.json`](./golden-vawa-prima-facie.json) (`fixture_version`: **2**)

It defines:
- primary VAWA I-360 + related unverified I-485  
- document aliases + production `document_id` / `content_hash` provenance contract  
- facts including `MARRIAGE_EXISTS` (verified) vs `SPOUSE_US_CITIZEN` (reported)  
- evidence gaps vs conflicts (this fixture has gaps, not manufactured conflicts)  
- event timeline vs current-status supersession; `I360_FINAL_DECISION` enum  
- action priority formula, 1–5 scales, scored ordered actions (resolve vs promote)  
- three locks; **allowed** anti-I-130 (required only for this regression fixture)  
- fact / legal interpretation (authority-backed) / implication  
- customer-text: **exact** for critical headings + forbidden contamination; **semantic** for legal meaning  
- invalidation + coalesce; approval BLOCKs/WARNINGs including stale + authority rules  
- plan honesty + aggregate reliability ceilings  

**Acceptance style:** structured state first; exact string matches only where contamination/heading regression demands it; semantic assertions for equivalent legal phrasing.

---

## 13. Phase scopes (implementation after freeze)

### F — Orchestration reliability
Root-cause failed-call / multi-run storm; logical analysis ID; enforce per-stage **and** aggregate ceilings; admin diagnostics.

### A — Classification + dedupe
Trace classifier vs renderer; fix wrong layer; dedupe by `content_hash` / document identity; plan-honesty when docs present.

### B — Fact ledger + invalidation
Provenance IDs; hierarchy; gaps vs conflicts; event timeline vs current-state supersession; resolve vs promote; rebuild on classification change with coalesce.

### C — Three locks
Retrieval / presentation / recommendation isolation; role-aware I-130/I-589; anti-I-130 allowed not always required.

### E — Approval gate
BLOCK/WARNING before customer-facing approve; stale + unsupported-authority rules; full audit trail.

### D — Action ranking
Deterministic additive priority formula; demote generic form review.

### G — Fixture pack (~8–12) with positive **and** negative isolation tests

| Case | Must allow | Must forbid |
| --- | --- | --- |
| VAWA I-360 + pending I-485 | I-360/I-485 framing | I-130 recommend; I-589 country conditions |
| Marriage I-130 / I-485 | I-130 as pathway | Suppressing I-130 globally |
| Asylum I-589 | I-589 / country conditions when locked | — |
| VAWA (neg) | — | I-589 country conditions |
| RFE / NOID / EAD / N-400 / consular / AOS w/o petition / court notice | case-appropriate | cross-contamination |

Every intelligence change runs the **full pack**.

---

## 14. Phase 0 exit criteria (freeze checklist)

Approve / freeze Phase 0 when you agree that:

- [ ] Diagnosis and non-regressions are correct  
- [ ] Sequence `0 → F → A → B → C → E → D → G` is locked  
- [ ] Marriage existence split from spouse citizenship  
- [ ] Gaps ≠ conflicts; event timeline ≠ current-state supersession  
- [ ] `I360_FINAL_DECISION` naming + enum  
- [ ] Production provenance joins on immutable IDs  
- [ ] Resolve unknown vs promote REPORTED→VERIFIED  
- [ ] Action formula + 1–5 scales control ordering  
- [ ] Anti-I-130 allowed, not globally required  
- [ ] Legal interpretation requires authority provenance  
- [ ] Invalidation + coalesce + stale BLOCKs  
- [ ] Aggregate reliability ceilings are numeric  
- [ ] Customer-text: exact for contamination/headings; semantic for legal meaning  
- [ ] Golden VAWA JSON v2 matches the intended contract  
- [ ] Positive + negative fixture strategy for Phase G is clear  

**After written freeze approval:** begin Phase F only (reliability). No A–D feature work until F exit criteria are met (or explicitly waived in writing for a parallel spike).

---

## 15. Explicit non-work in V5.1

- New customer marketing UI / layout redesign  
- Broad prompt rewrite as the primary fix  
- USCIS filing / G-28 / mail-packet product work  
- Turning off the approval gate to “ship faster”  
- Habitual “don’t file I-130” on every VAWA case when no competing path is in play  

---

*End of Phase 0 specification (rev 2).*
